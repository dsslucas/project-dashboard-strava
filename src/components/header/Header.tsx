import React from "react"
import Navbar from "../navbar/Navbar";

const Header = () => {
    return (
        <header className="h-16">
            <Navbar />
        </header>
    );
}

export default Header;